import { useState } from "react";

function App() {
    const [] = useState(null);

    return (
        <div style={{ padding: "2rem" }}>
            <h1>Quotes API (React + Vite)</h1>

        </div>
    );
}

export default App;
